export const DEFAULT_MENU_TIMEOUT_MS = 5 * 60 * 1000; // 5 minutes
export const POLL_INTERVAL_MS = 500;
export const INTERACTIVE_MENU_DESCRIPTION =
  "Show a terminal menu with options. A tmux window opens visibly — switch to it with Ctrl+b n (next window). User picks from the list or types free text, presses Enter. Returns the user's input as JSON. Use for structured choices when you need user input.";
